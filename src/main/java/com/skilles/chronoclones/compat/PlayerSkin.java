//? if <1.20.2 {
/*package com.skilles.chronoclones.compat;

import net.minecraft.resources.ResourceLocation;

// The slice of 1.20.2's PlayerSkin that the renderer uses: a texture and its arm width.
public record PlayerSkin(ResourceLocation texture, Model model) {

    public enum Model {
        WIDE,
        SLIM
    }
}
*///?}
