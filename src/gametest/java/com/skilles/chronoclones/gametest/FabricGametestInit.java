//? if fabric {
/*package com.skilles.chronoclones.gametest;

import net.fabricmc.api.ModInitializer;

// Entrypoint of the dev-only chronoclones_gametest mod (see the gametest fabric.mod.json).
public class FabricGametestInit implements ModInitializer {

    @Override
    public void onInitialize() {
        ChronoclonesGameTests.registerFunctions();
    }
}
*///?}
